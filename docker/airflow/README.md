# Airflow

TODO: fill this readme with relevant info. This file existance ensures that this directory is created for the project.

make sure you create a directory called `../docker/airflow/logs` for this project. Otherwise the whole thing won't work.